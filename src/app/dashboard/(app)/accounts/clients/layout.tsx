import type { Metadata } from 'next';

export const metadata: Metadata = {
 title: 'Accounts clients | HRIS Demo',
};

export default function AccountsClientsLayout({ children }: { children: React.ReactNode }) {
 return children;
}
