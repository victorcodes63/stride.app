# Stride brand assets

| File | Use |
|------|-----|
| `stride-mark.svg` | Wing gradient mark — favicon, sidebar icon, compact UI |
| `stride-wordmark.svg` | Full logotype — marketing headers, auth panels, OG/social |

Constants in `src/lib/brand-constants.ts`: `STRIDE_MARK_SRC`, `STRIDE_WORDMARK_SRC`.

Official vector assets live here as SVG (`stride-mark.svg`, `stride-wordmark.svg`). Legacy PNG exports may remain for favicon/OG fallbacks.
